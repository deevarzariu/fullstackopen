import { createSlice } from "@reduxjs/toolkit";

const initialState = "render here notification...";

const notificationSlice = createSlice({
  name: "render here notification...",
  initialState,
  reducers: {},
});

export default notificationSlice.reducer;
